To configure:

```bash
eww close-all # close the current bar
eww --config $HOME/.dotfiles-nix/modules/home-manager/eww open bar
eww --config $HOME/.dotfiles-nix/modules/home-manager/eww close-all
```
