promptToReturnFromSubprocess: false
gui:
  showIcons: true
git:
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never
  commitPrefixes:
    auditboard-frontend:
      - pattern: "^(SOX-[0-9]{1,}).*"
        replace: "$1"
    soxhub-api:
      - pattern: "^(SOX-[0-9]{1,}).*"
        replace: "$1"
